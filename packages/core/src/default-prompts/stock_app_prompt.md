Build me a stock market dashboard as a DeskTalk LiveApp.

Requirements:

- Let me enter and track a watchlist of stock ticker symbols.
- Show price, daily change, percent change, and last updated time for each symbol.
- Include a simple chart view for the selected stock if an accessible data source is available.
- If real market data is unavailable, fall back gracefully with mock data and make that clear in the UI.
- Save the watchlist locally with the DeskTalk storage bridge so it persists.
- Create a responsive layout that works inside a DeskTalk window.
- Make the UI feel like a focused financial dashboard, not a generic table.
- Follow the DeskTalk manual for styling, components, and platform conventions.

Please create the LiveApp directly.
